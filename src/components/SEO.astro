---
interface Props {
    title?: string
    description?: string
    canonical?: string
    noIndex?: boolean
}

const {
    title,
    description = 'Vessel is a self-hosted database panel that handles networking, isolated environments, and one-click deployment for any database — no installation required.',
    canonical,
    noIndex = false,
} = Astro.props

const siteUrl = 'https://vessel.wiki'
const siteName = 'Vessel'
const defaultTitle = 'Vessel — Self-Hosted Database Panel'
const resolvedTitle = title ? `${title} — Vessel` : defaultTitle
const resolvedCanonical = canonical ?? new URL(Astro.url.pathname, siteUrl).href
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>{resolvedTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={resolvedCanonical} />
{noIndex && <meta name="robots" content="noindex, nofollow" />}

<link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="shortcut icon" href="/favicon.ico" />
<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
<meta name="apple-mobile-web-app-title" content="Vessel" />
<link rel="manifest" href="/site.webmanifest" />

<!-- OG -->
<meta property="og:type" content="website" />
<meta property="og:site_name" content={siteName} />
<meta property="og:url" content={resolvedCanonical} />
<meta property="og:title" content={resolvedTitle} />
<meta property="og:description" content={description} />

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={resolvedTitle} />
<meta name="twitter:description" content={description} />
